# =============================================================================
# Dependências Python para o runtime serverless da Vercel.
# Versão slim do backend/requirements.txt — sem ferramentas de dev/teste.
# =============================================================================

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32

# --- Banco de dados (PostgreSQL via asyncpg) ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14

# --- Validação ---
pydantic[email]>=2.9
pydantic-settings>=2.6

# --- Autenticação ---
python-jose[cryptography]>=3.3
argon2-cffi>=23.1
passlib[bcrypt]>=1.7

# --- Rate limiting ---
slowapi>=0.1.9

# --- Adaptador ASGI → AWS Lambda (Vercel serverless) ---
mangum>=0.17

# --- Utilitários ---
greenlet>=3.0
