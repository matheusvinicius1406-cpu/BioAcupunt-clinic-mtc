# Default placeholder values for secrets-gradle-plugin.
# This file IS committed to git (safe placeholder values only).
# Real keys go in local.properties (gitignored) for local dev,
# or are injected via CI secrets for release builds.
GEMINI_API_KEY=
