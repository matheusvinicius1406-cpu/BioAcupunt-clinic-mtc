# Add project specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified in the Android SDK tools proguard files.

# Keep Retrofit interfaces (when reintroduced)
# -keep interface com.bioacupunt.data.remote.** { *; }

# Keep Room entities
# -keep class com.bioacupunt.data.local.model.** { *; }
