# =============================================================================
# Legacy — kept for local dev reference.
# Vercel now reads pyproject.toml (root) for dependencies via `uv`.
# Root pyproject.toml is the single source of truth for Vercel deploys.
# backend/requirements.txt still used for local dev + tests.
# =============================================================================

fastapi>=0.115
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
pydantic[email]>=2.9
pydantic-settings>=2.6
python-jose[cryptography]>=3.3
argon2-cffi>=23.1
slowapi>=0.1.9
greenlet>=3.0
